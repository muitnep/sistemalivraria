spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sistemalivraria
    username: usuario_livraria
    password: 'EaGb%W6Q19-i"x$`'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect